
const Btn = (prop) => {
  return (
    <a href="#" className="hidden md:block w-fit bg-lightRed px-3 py-2 text-md uppercase font-medium rounded-full text-white hover:bg-bsBlue transition-all duration-[.3s] ease-in-out">{prop.text}</a>
  )
}

export default Btn