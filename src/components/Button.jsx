
const Button = (prop) => {
  return (
    <button href="#" className=" bg-lightRed p-3 uppercase font-medium rounded-3xl text-white hover:bg-bsBlue transition-all duration-[.3s] ease-in-out">{prop.text}</button>
  )
}

export default Button